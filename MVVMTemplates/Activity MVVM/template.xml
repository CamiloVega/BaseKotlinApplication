<?xml version="1.0"?>
<template format="5"
        revision="1"
        name="MVVM Activity"
        description="Creates a new MVVM Activity (DONT FORGET TO ADD TO VIEWBINDINGMODULE!!!)">
    
    <category value="Activity"/>
 
    <parameter id="className"
        name="Functionality Name"
        type="string"
        constraints="class|unique|nonempty"
        default="FeatureName"
        help="The name of the functionality or feature"/>

    <parameter
        id="includeLayout"
        name="Create layout XML?"
        type="boolean"
        default="true"
        help="Generate a layout XML for the activity" />

    <parameter
        id="fragmentName"
        name="Activity Layout Name"
        type="string"
        constraints="layout|nonempty|unique"
        default="activity_blank"
        visibility="includeLayout"
        suggest="activity_${classToResource(className)}"
        help="The name of the layout to create" />
 
    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />
 
</template>